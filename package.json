{
  "name": "xrp-pay-router",
  "version": "0.1.0",
  "private": true,
  "description": "XRP-Pay Router - payment-gated AI gateway that routes agents to the cheapest capable LLM, settled in XRP/RLUSD via the x402 protocol.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @xrppay/router-core && npm run build -w @xrppay/server",
    "test": "npm run build && npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.2"
  }
}
